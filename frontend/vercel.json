{
  "buildCommand": "yarn build",
  "outputDirectory": "build",
  "installCommand": "yarn install --network-timeout 120000",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
